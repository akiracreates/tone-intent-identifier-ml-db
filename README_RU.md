# 📄 Техническая документация  
# «Tone & Intent Identifier: ML + DB Suite»  
Автор: **Хаггаг Амира / Amira Haggag**  
Группа: **ИСП9-Kh32**

> Курсовой проект по профессиональным модулям:  
> **МДК 01.01 «Разработка модулей программного обеспечения для компьютерных систем»**  
> **МДК 11.01 «Технология разработки и защиты баз данных»**

Данный программный комплекс представляет собой интегрированную систему анализа коротких текстовых сообщений, объединяющую:

- **подсистему машинного обучения (ML)** для автоматического определения *тона* и *намерения* сообщения;  
- **подсистему базы данных (DB)** на основе PostgreSQL для хранения сообщений, результатов инференса и выполнения аналитических SQL‑запросов.

Система разработана с образовательной целью и демонстрирует полный цикл разработки интеллектуального программного обеспечения:  
от подготовки данных и обучения моделей — до интеграции инференса с реляционной СУБД.

---

# 1. Назначение и область применения

Проект предназначен для:

- классификации коротких пользовательских сообщений;  
- идентификации коммуникационного типа текста (дружелюбный, формальный, негативный и т.д.);  
- классификации намерения автора (запрос, жалоба, инструкция, уточнение и др.);  
- хранения данных и результатов анализа в PostgreSQL;  
- выполнения аналитики для отчётности и исследования модели.

Комплекс может использоваться:

- студентами и преподавателями как учебный инструмент;  
- в курсовых и дипломных проектах;  
- как прототип для интеграции моделей NLP в реальные системы.

---

# 2. Архитектура и компоненты системы

## 2.1. Подсистема машинного обучения (ML)

Включает:

- подготовку датасетов;  
- построение токенизатора (`tokenizer.pkl`);  
- обучение двух моделей тональности:
  - **BiLSTM** — хорошо обрабатывает последовательности;  
  - **CNN** — быстро выявляет локальные паттерны речи;  
- обучение модели намерений:
  - **TF‑IDF + Logistic Regression** — интерпретируемое и устойчивое решение;  
- модуль инференса (`predict.py`);
- генератор синтетических данных (`generate_eval_messages_1000.py`).

## 2.2. Подсистема базы данных (DB)

Состоит из:

- реляционной схемы (`schema.sql`):  
  - `messages` — исходные тексты;  
  - `predictions` — результаты инференса;  
- загрузчика данных (`load_results.py`);  
- аналитических SQL‑запросов (`db_project/queries/`).

База данных играет роль хранилища, аналитического слоя и основы для последующей визуализации.

---

# 3. Категории классификации

## 3.1. Тональность (Tone)

Шесть классов:

- neutral  
- positive_friendly  
- negative_rude  
- sarcastic_ironic  
- formal  
- informal_casual  

## 3.2. Намерение (Intent)

Шесть классов:

- request  
- command_instruction  
- complaint  
- praise_appreciation  
- clarification_question  
- statement_information  

---

# 4. Требования к программной среде

## 4.1. Необходимые компоненты

- Python 3.10+  
- PostgreSQL 14/15  
- pgAdmin (рекомендуется)  

## 4.2. Используемые библиотеки Python

- numpy  
- pandas  
- scikit‑learn  
- tensorflow  
- matplotlib  
- joblib  

---

# 5. Установка и развертывание

## 5.1. Клонирование проекта

```bash
git clone https://github.com/<username>/tone-intent-identifier-ml-db.git
cd tone-intent-identifier-ml-db
```

## 5.2. Настройка виртуального окружения

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

---

# 6. Конфигурация базы данных PostgreSQL

## 6.1. Создание базы

```sql
CREATE DATABASE tone_intent_db;
```

## 6.2. Настройка переменных среды

```powershell
setx DB_HOST "localhost"
setx DB_PORT "5432"
setx DB_NAME "tone_intent_db"
setx DB_USER "<user>"
setx DB_PASSWORD "<password>"
```

## 6.3. Применение схемы

```bash
psql -U <user> -d tone_intent_db -f db_project/schema.sql
```

Таблицы:

- `messages(id, text, created_at)`  
- `predictions(id, message_id, tone_bilstm_pred, tone_cnn_pred, intent_pred, predicted_at)`

---

# 7. Обучение ML‑моделей

## 7.1. Модели тональности

### Токенизация и подготовка данных

```bash
python -m ml_project.tokenize_and_train.build_tokenizer_and_sequences
python -m ml_project.tokenize_and_train.prepare_balanced_tone_datasets
```

### Обучение моделей

```bash
python -m ml_project.tokenize_and_train.train_tone_lstm
python -m ml_project.tokenize_and_train.train_tone_cnn
```

## 7.2. Модель намерений

```bash
python -m ml_project.tokenize_and_train.train_intent_tfidf_logreg_balanced
```

---

# 8. Оценка моделей

```bash
python -m ml_project.evaluate --task all
```

Генерируются:

- отчёты `.txt`;  
- матрицы ошибок `.png`;  
- метрики F1, Accuracy и др.

---

# 9. Инференс (предсказания)

## 9.1. Одно сообщение

```bash
python -m ml_project.predict --text "Пример сообщения"
```

## 9.2. Пакетный анализ CSV

```bash
python -m ml_project.predict ^
  --input data/examples/eval_messages_1000.csv ^
  --output outputs/predictions/eval_predictions_1000.csv
```

---

# 10. Генерация синтетического датасета

Скрипт:

```bash
python -m ml_project.generate_eval_messages_1000
```

Выходной файл:

```
data/examples/eval_messages_1000.csv
```

Служит для аналитики внутри PostgreSQL.

---

# 11. Загрузка предсказаний в PostgreSQL

```bash
python -m db_project.load_results ^
  --input outputs/predictions/eval_predictions_1000.csv
```

Сценарий:

- добавляет текст в `messages`;  
- связывает предсказание через `message_id`;  
- загружает результаты за одну операцию (bulk insert).

---

# 12. Аналитика SQL

Каталог:

```
db_project/queries/
```

Примеры запросов:

### 12.1. Распределение тональности

```sql
SELECT tone_bilstm_pred, COUNT(*) 
FROM predictions 
GROUP BY tone_bilstm_pred;
```

### 12.2. Сравнение моделей BiLSTM и CNN

```sql
SELECT 
    SUM(CASE WHEN tone_bilstm_pred = tone_cnn_pred THEN 1 END) AS agree,
    SUM(CASE WHEN tone_bilstm_pred <> tone_cnn_pred THEN 1 END) AS disagree
FROM predictions;
```

### 12.3. Матрица «намерение ↔ тон»

```sql
SELECT intent_pred, tone_bilstm_pred, COUNT(*)
FROM predictions
GROUP BY intent_pred, tone_bilstm_pred;
```

SQL‑аналитика используется в курсовой работе для оценки стабильности и логичности модели.

---

# 13. Структура репозитория

```
tone-intent-identifier-ml-db/
├─ ml_project/
│  ├─ tokenize_and_train/
│  ├─ dataset_scripts/
│  ├─ generate_eval_messages_1000.py
│  ├─ evaluate.py
│  └─ predict.py
│
├─ db_project/
│  ├─ schema.sql
│  ├─ load_results.py
│  └─ queries/
│
├─ data/
│  ├─ examples/
│  ├─ raw/ (ignored)
│  └─ processed/ (ignored)
│
├─ outputs/ (ignored)
├─ README.md
├─ README_RU.md
└─ requirements.txt
```

---

# 14. Заключение

Система **Tone & Intent Identifier: ML + DB Suite** демонстрирует полный цикл разработки интеллектуального программного комплекса, включающего машинное обучение, инференс, хранение данных и аналитические SQL‑запросы.  
Проект полностью соответствует требованиям курсовых работ по:

- **МДК 01.01**  
- **МДК 11.01**

и может служить основой для дальнейших исследований, расширений и внедрения.

---

# 15. Контактная информация

**Автор:** Хаггаг Амира / Amira Haggag  
Группа: ИСП9-Kh32  
Email: akiracreates.comms@gmail.com  
GitHub: https://github.com/akiracreates
