# 📄 Руководство пользователя (README_RU.md)

# Система идентификации тона и намерения текста

# «Tone & Intent Identifier: ML + DB Suite»

------------------------------------------------------------------------

# 1. Общая характеристика проекта

Данный программный комплекс разработан в рамках курсовых проектов по
двум профессиональным модулям специальности **09.02.07 «Информационные
системы и программирование»**:

-   **МДК 01.01 «Разработка модулей программного обеспечения для
    компьютерных систем»**\
-   **МДК 11.01 «Технология разработки и защиты баз данных»**

Проект представляет собой интегрированную систему, объединяющую:

1.  **Модуль машинного обучения (ML)** --- предназначен для
    автоматической классификации пользовательских текстов по двум
    критериям:
    -   *тональность (tone)*\
    -   *намерение (intent)*
2.  **Базу данных PostgreSQL (DB-субъект)** --- обеспечивает хранение
    сообщений, истинных меток, предсказаний моделей, журналов обработки
    данных и аналитических SQL-запросов.

Обе части системы спроектированы так, чтобы их можно было использовать
как **единый законченный программный комплекс** для анализа текстовых
данных.

------------------------------------------------------------------------

# 2. Назначение и функции системы

## 2.1. Назначение

Система предназначена для:

-   обработки коротких текстовых сообщений (чат-сообщения, комментарии,
    электронные письма);
-   автоматической классификации текста:
    -   по **тону сообщения** (модель на основе нейронных сетей);
    -   по **намерению автора** (классический ML-алгоритм);
-   хранения информации о сообщениях и их классификации в **реляционной
    базе данных**;
-   выполнения аналитических SQL-запросов для оценки точности моделей и
    качества данных.

## 2.2. Основные функции

Система предоставляет пользователю следующие функциональные возможности:

-   загрузка и предварительная обработка текстовых данных;
-   балансировка датасетов по классам тональности и намерений;
-   построение токенизатора и числовых представлений текста;
-   обучение моделей:
    -   **BiLSTM** и **CNN** для определения тона;
    -   **TF-IDF + Logistic Regression** для классификации намерений;
-   формирование тестовой выборки и обеспечение воспроизводимой оценки
    моделей;
-   получение предсказаний для новых сообщений;
-   загрузка результатов классификации в базу данных PostgreSQL;
-   выполнение SQL-скриптов для получения аналитической статистики.

------------------------------------------------------------------------

# 3. Система меток (Tone & Intent)

## 3.1. Метки тональности (Tone)

В состав классификатора тональности входят 6 категорий:

1.  `neutral`\
2.  `positive_friendly`\
3.  `negative_rude`\
4.  `sarcastic_ironic`\
5.  `formal`\
6.  `informal_casual`

Для классификации используются две нейросетевые архитектуры:

-   **BiLSTM-модель**
-   **CNN-модель**

## 3.2. Метки намерений (Intent)

Классификатор намерений предсказывает одну из 6 категорий:

7.  `request`\
8.  `command_instruction`\
9.  `complaint`\
10. `praise_appreciation`\
11. `clarification_question`\
12. `statement_information`

Модель намерений основана на комбинации:

-   TF-IDF векторизации текста\
-   Логистической регрессии\
-   Процедуры балансировки обучающей части датасета

------------------------------------------------------------------------

# 4. Требования к программной среде

## 4.1. Программное обеспечение

-   **Python 3.10+**
-   **PostgreSQL 14/15**
-   Git
-   Рекомендуемые библиотеки Python:
    -   `numpy`
    -   `pandas`
    -   `scikit-learn`
    -   `tensorflow`
    -   `matplotlib`
    -   `joblib`

## 4.2. Аппаратная среда

-   Стандартный ноутбук или ПК\
-   ОС Windows 10 (среда разработки)

------------------------------------------------------------------------

# 5. Установка и настройка

## 5.1. Клонирование репозитория

``` bash
git clone https://github.com/<username>/tone-intent-identifier-ml-db.git
cd tone-intent-identifier-ml-db
```

## 5.2. Создание виртуального окружения

``` bash
python -m venv .venv
.venv\Scripts\activate
```

## 5.3. Установка зависимостей

``` bash
pip install -r requirements.txt
```

## 5.4. Настройка PostgreSQL

1.  Создание базы данных:

``` sql
CREATE DATABASE tone_intent_db;
```

2.  Настройка переменных среды:

``` powershell
setx DB_HOST "localhost"
setx DB_PORT "5432"
setx DB_NAME "tone_intent_db"
setx DB_USER "<your_user>"
setx DB_PASSWORD "<your_password>"
```

3.  Применение схемы:

``` bash
psql -U <your_user> -d tone_intent_db -f db_project/schema.sql
```

------------------------------------------------------------------------

# 6. Подготовка данных

## 6.1. Балансировка датасетов

``` bash
python -m ml_project.dataset_scripts.rebalance_tone_intent
```

Создаются:

-   `tone_balanced_for_training.csv`
-   `intent_balanced_for_training.csv` *(в финальной системе не
    используется)*

## 6.2. Построение итогового датасета намерений

``` bash
python -m ml_project.dataset_scripts.build_intent_final_dataset
```

Создаётся файл:

`data/processed/intent_final_dataset.csv`

------------------------------------------------------------------------

# 7. Обучение моделей

## 7.1. Тональность

### Шаг 1 --- построение токенизатора

``` bash
python -m ml_project.tokenize_and_train.build_tokenizer_and_sequences
```

### Шаг 2 --- создание обучающей, валидационной и тестовой выборок

``` bash
python -m ml_project.tokenize_and_train.prepare_balanced_tone_datasets
```

### Шаг 3 --- обучение моделей

BiLSTM:

``` bash
python -m ml_project.tokenize_and_train.train_tone_lstm
```

CNN:

``` bash
python -m ml_project.tokenize_and_train.train_tone_cnn
```

## 7.2. Намерение

``` bash
python -m ml_project.tokenize_and_train.train_intent_tfidf_logreg_balanced
```

Скрипт выполняет:

-   стратифицированное разбиение данных,
-   сохранение тестовой выборки,
-   балансировку обучающей части,
-   обучение модели,
-   запись отчёта и сохранение модели.

------------------------------------------------------------------------

# 8. Оценка моделей

## 8.1. Полная оценка

``` bash
python -m ml_project.evaluate --task all
```

## 8.2. Выборочная оценка

-   BiLSTM:

``` bash
python -m ml_project.evaluate --task tone_bilstm
```

-   CNN:

``` bash
python -m ml_project.evaluate --task tone_cnn
```

-   Intent:

``` bash
python -m ml_project.evaluate --task intent
```

Результаты сохраняются в:

    outputs/metrics/

------------------------------------------------------------------------

# 9. Предсказание на новых данных

Одиночное сообщение:

``` bash
python -m ml_project.predict --text "Пример сообщения пользователя"
```

Пакетная обработка:

``` bash
python -m ml_project.predict ^
  --input data/processed/messages_clean.csv ^
  --output outputs/predictions/predictions.csv
```

------------------------------------------------------------------------

# 10. Загрузка результатов в базу данных

``` bash
python -m db_project.load_results ^
  --messages data/processed/messages_clean.csv ^
  --predictions outputs/predictions/predictions.csv
```

------------------------------------------------------------------------

# 11. Аналитические SQL-запросы

``` bash
psql -d tone_intent_db -f db_project/queries/tone_accuracy_by_label.sql
```

------------------------------------------------------------------------

# 12. Структура репозитория

    tone-intent-identifier-ml-db/
    ├─ ml_project/
    │  ├─ dataset_scripts/
    │  ├─ tokenize_and_train/
    │  ├─ evaluate.py
    │  └─ predict.py
    │
    ├─ db_project/
    │  ├─ schema.sql
    │  ├── load_results.py
    │  └── queries/
    │
    ├─ data/
    │  ├─ raw/
    │  ├─ processed/
    │
    ├─ outputs/
    ├─ README.md
    ├─ README_RU.md
    └─ requirements.txt

------------------------------------------------------------------------

# 13. Автор

**Хаггаг Амира / Amira Haggag**\
Группа **ИСП9-Kh32**

Роли в проекте:

-   разработка модуля машинного обучения,
-   построение и документирование базы данных,
-   подготовка аналитических материалов,
-   написание технической документации (EN + RU).

------------------------------------------------------------------------

# 14. Контакты

-   Email: **akiracreates.comms@gmail.com**\
-   Telegram: **https://t.me/akiracreates**\
-   GitHub: **https://github.com/akiracreates**
